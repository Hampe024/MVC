<!DOCTYPE html>
<html>
    <link rel="stylesheet" href="{{ asset("css/style.css") }}"/>

        

    <body>
        <div class="outer-nav"></div>
        <div class="wrapper">
            <div class="navbar">
                <a href="{{ path("home") }}">Home</a>
                <a href="{{ path("about") }}">Om MVC</a>
                <a href="{{ path("report") }}">Redovisning</a>
                <a href="{{ path("card") }}">Kortspel</a>
            </div>
            {% block content %}
                default
            {% endblock %}
        </div>
        <div class="footer">
            Website made with symfony
        </div>
    </body>

</html>



